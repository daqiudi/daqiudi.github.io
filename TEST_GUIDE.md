# 功能测试指南

## 测试前准备

1. 启动本地服务器：
   ```bash
   # Windows
   start.bat
   
   # 或者手动启动
   python -m http.server 8080
   ```

2. 在浏览器中访问：`http://localhost:8080`

---

## 测试项目 1：照片分组侧边栏完全隐藏功能

### 测试步骤

#### 状态1：展开 → 折叠
1. 找到左侧的照片分组侧边栏
2. 点击顶部的 **▼** 按钮
3. **预期结果**：
   - 侧边栏宽度缩小
   - 只显示图标（📸、💕等）
   - 文字和数量被隐藏
   - 按钮变为 **◀**

#### 状态2：折叠 → 完全隐藏
4. 再次点击 **◀** 按钮
5. **预期结果**：
   - 侧边栏变成一个小圆形按钮
   - 所有分组内容完全隐藏
   - 只显示 **▶** 图标
   - 侧边栏宽度约50px

#### 状态3：完全隐藏 → 展开
6. 点击 **▶** 按钮
7. **预期结果**：
   - 侧边栏恢复到完整展开状态
   - 显示所有分组的完整信息
   - 按钮恢复为 **▼**

### 测试要点
- ✅ 三次点击循环切换三个状态
- ✅ 图标变化正确（▼ → ◀ → ▶ → ▼）
- ✅ 过渡动画流畅
- ✅ 移动端也能正常使用

---

## 测试项目 2：移动端导航栏横向滑动

### 桌面端测试（使用鼠标）

1. 打开浏览器开发者工具（F12）
2. 切换到移动设备模拟模式（Ctrl+Shift+M）
3. 选择一个手机型号（如 iPhone 12 Pro）
4. 尝试以下操作：

#### 鼠标拖动测试
5. 将鼠标移到顶部导航栏的菜单区域
6. 按住鼠标左键并向左/右拖动
7. **预期结果**：
   - 菜单项跟随鼠标移动
   - 可以查看所有被遮挡的菜单项
   - 鼠标指针变为抓取手势（grab/grabbing）
   - 滚动平滑，无卡顿

### 真实移动端测试

#### 触摸滑动测试
1. 在手机浏览器中打开页面
2. 找到顶部导航栏
3. 用手指左右滑动菜单区域
4. **预期结果**：
   - 菜单项跟随手指滑动
   - 可以顺畅查看所有菜单项
   - 滑动响应灵敏
   - 无滚动条显示
   - 不会误触点击按钮

### 测试要点

#### 视觉检查
- ✅ 滚动条完全隐藏
- ✅ 菜单项不会被压缩变形
- ✅ 所有按钮都能正常点击
- ✅ 滑动时不会出现选择文本

#### 交互检查
- ✅ 滑动流畅，无卡顿
- ✅ 可以快速滑动多个菜单项
- ✅ 滑动后仍能正常点击按钮
- ✅ 不会与页面其他滚动冲突

#### 兼容性检查
- ✅ Chrome/Edge 浏览器
- ✅ Firefox 浏览器
- ✅ Safari（iOS）
- ✅ Android 默认浏览器

---

## 常见问题排查

### 问题1：侧边栏无法完全隐藏
**可能原因**：CSS文件未正确加载
**解决方法**：
1. 清除浏览器缓存（Ctrl+F5）
2. 检查控制台是否有CSS错误
3. 确认 `photo-wall.css` 文件路径正确

### 问题2：导航栏无法滑动
**可能原因**：JavaScript未正确初始化
**解决方法**：
1. 打开浏览器控制台（F12）
2. 查看是否有JavaScript错误
3. 确认 `navigation.js` 已正确加载
4. 检查 `initMobileSwipe()` 是否被调用

### 问题3：移动端滑动不流畅
**可能原因**：触摸事件处理问题
**解决方法**：
1. 确认使用了 `{ passive: true }` 选项
2. 检查是否有其他事件监听器冲突
3. 尝试在不同设备上测试

### 问题4：桌面端鼠标拖动无效
**可能原因**：鼠标事件绑定失败
**解决方法**：
1. 检查控制台错误
2. 确认 `.nav-sections` 元素存在
3. 验证事件监听器是否正确添加

---

## 性能测试

### 内存使用
- 打开任务管理器监控内存
- 多次切换侧边栏状态
- 观察内存是否正常释放

### 滑动性能
- 快速连续滑动导航栏
- 观察帧率是否稳定（应保持60fps）
- 检查是否有明显卡顿

---

## 回归测试

确保新功能不影响现有功能：

- ✅ 照片浏览功能正常
- ✅ 照片分组筛选正常
- ✅ 播放模式正常
- ✅ 上传功能正常
- ✅ 设置面板正常
- ✅ 全屏功能正常
- ✅ 键盘快捷键正常
- ✅ 响应式布局正常

---

## 测试完成清单

- [ ] 桌面端侧边栏三态切换测试通过
- [ ] 移动端侧边栏三态切换测试通过
- [ ] 桌面端鼠标拖动导航测试通过
- [ ] 移动端触摸滑动导航测试通过
- [ ] 多浏览器兼容性测试通过
- [ ] 性能测试通过
- [ ] 回归测试通过
- [ ] 无明显Bug

---

## 反馈与改进

如果发现问题，请记录：
1. 问题描述
2. 复现步骤
3. 设备和浏览器信息
4. 截图或录屏（如有）
